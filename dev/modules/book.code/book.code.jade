- var jmAppUtils = (function(){ var module = {};
    - module.escapeHtml = function(html){return html.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");};
- return module;}());

- var jmBookCode = (function(){ var module = {};
    - module.code = function(source){return '<code>'+jmAppUtils.escapeHtml(source)+'</code>'};
    - module.relevant = function(source){return '<code class="book-code-relevant">'+jmAppUtils.escapeHtml(source)+'</code>'};
    - module.comment = function(source){return '<code class="book-code-comment">'+jmAppUtils.escapeHtml(source)+'</code>'};
- return module; }());
